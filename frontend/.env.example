# Only needed if your backend is deployed separately from the frontend
# (e.g. backend on Render, frontend on Vercel). Leave unset for local dev —
# Vite's dev proxy already forwards /api requests to http://localhost:5000
VITE_API_URL=http://localhost:5000/api/chat
